import React, { Component } from 'react';

class Undo extends Component {
    render() {
        return(
            <div className="undoContainer">
                <button>Undo</button>
            </div>
        )
    }
}

export default Undo;