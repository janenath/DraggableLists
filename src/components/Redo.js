import React, { Component } from 'react';

class Redo extends Component {
    render() {
        return(
            <div className="redoContainer">
                <button>Redo</button>
            </div>
        )
    }
}

export default Redo;